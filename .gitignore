node_modules/
.next/
.next-b/
out/
build/
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env*.local
next-env.d.ts
tsconfig.tsbuildinfo

# Runtime data — never commit. The update system relies on these being
# untracked so that `git` operations can never touch live data.
/data/
/backups/
/logs/

# Local tool configuration - not part of the app
.claude/
